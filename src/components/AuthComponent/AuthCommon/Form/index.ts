import FormWrapper from './FormWrapper';
import Input from './Input';
import SubmitButton from './SubmitButton';

const Form = {
    FormWrapper,
    Input,
    SubmitButton,
};

export default Form;
