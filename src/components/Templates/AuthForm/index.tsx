import AuthFormContainer from "./Form";
import AUthFormSubmitButton from "./SubmitButton";

const AuthForm = {
  Form: AuthFormContainer,
  SubmitButton: AUthFormSubmitButton,
};

export default AuthForm;
